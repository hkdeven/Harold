- content_for(:alerts) do
  .card.copy--body{style: "padding: 50px; margin: 50px auto"}
    .copy--h2 This is the alert area

.row
  .row__inner
    .card.copy--body.context--content{style: "padding: 20px"}
      %span.copy--h1 This is the minimal layout
      %p This layout is used for simple clients
